# LangFuse
LANGFUSE_PORT=3001
LANGFUSE_LAN_IP=192.168.x.x # Homelab laptop IP

# This needs to point to your global PostgreSQL instance.
# You must create the 'langfuse' database manually in PostgreSQL before starting.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:StrongLocalPassword@192.168.x.x:5432/langfuse

# Generate this with `openssl rand -base64 32` and keep it secret
LANGFUSE_SECRET=

# On the first run, the server will generate and log a public and secret key.
# Paste them here for your applications to use.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
